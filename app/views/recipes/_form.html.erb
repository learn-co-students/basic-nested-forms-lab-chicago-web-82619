<%= form_for @recipe do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>

  <%= f.fields_for :ingredients do |i| %>
    <%= i.label :name %>
    <%= i.text_field :name %><br>

    <%= i.label :quantity %>
    <%= i.text_field :quantity %><br>

  <% end %>

  <%= f.submit %>
<% end %>

recipe"=>{
  "title"=>"banana smoothie",
  "ingredients_attributes"=>{
    "0"=>{
      "name"=>"banana",
      "quantity"=>"1 banana",
      "recipe_id"=>"1"
    }, "1"=>{
      "name"=>"milk",
      "quantity"=>"lots of milk",
      "recipe_id"=>"2"
    }
  }
